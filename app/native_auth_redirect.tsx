import { View } from 'react-native';

// This screen is intentionally left blank.
// It acts as a placeholder to prevent Expo Router's
// "Unknown path" error during Clerk's native OAuth redirects.
// Clerk handles the actual redirect logic internally.
export default function NativeAuthRedirect() {
  return <View />;
} 